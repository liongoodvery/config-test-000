spring:
  profiles:
    active: dev
org:
  lion:
    info:
      name: default-config
---
spring:
  profiles: dev
  application:
    name: provider-dev

---
spring:
  profiles: test
  application:
    name: provider-test